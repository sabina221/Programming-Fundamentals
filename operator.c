/*
name:sabina prajapati
roll no:
lab no:
program:WAP to use the operators
date:
*/

#include<stdio.h>
  int main(){
	 int x,y;
	 printf("enter the value of x\n");
	 scanf("%d",&x);
	 printf("enter the value of y\n");
	 scanf("%d",&y);
	
	 printf("use of operator ==  is: %d\n", x==y);
	 
	 printf("use of operator > is:%d\n",x>y);
	 
	 printf("use of operator < is:%d\n",x<y);
	
	 printf("use of operator != is:%d\n",x!=y);
	 
	 printf("use of operator >=  is: %d\n",x>=y);
	
	 printf("use of operator <= is: %d\n",x<=y);
	 return 0;
	 }
	 	 
	
